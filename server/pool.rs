// use actix_web::web::{self, get, Data};
// use actix_web::{App, HttpServer};
// use dotenv::dotenv;
// use std::env;
// use std::io::Result as IOResult;

// pub fn server() -> IOResult<()> {
//     dotenv().ok();

//     let server = HttpServer::new(move || {
//       App::new()
//         .app_data(Data::new(schemas))
//     });

//     Ok(())
// }
